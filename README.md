# SQL Subqueries and Nested Queries

This project demonstrates the use of SQL subqueries in **SELECT**, **WHERE**, and **FROM** clauses. It includes examples of scalar, correlated, and nested subqueries for filtering and data retrieval.

## Technologies
- MySQL / SQLite
- SQL

## Author
**Ridhi Saxena**
